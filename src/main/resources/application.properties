# Database Configuration
spring.datasource.url=jdbc:postgresql://immunedb.crs4iokyoby5.ap-northeast-2.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres.wtvubemvnfminxvctdfo
spring.datasource.password=choido0424@
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# SSL Configuration
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# Other configurations
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JPA Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging Configuration
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Server Configuration
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# File Configuration
file.path=C:/uploads/
file.url=http://52.79.173.199:8080/file/

# JWT Configuration
secret-key=S3cr3tK3yS3cr3tK3yS3cr3tK3y1234567890S3cr3tK3y

# HTTP 메소드 허용 설정
spring.mvc.hiddenmethod.filter.enabled=true

# 추가 설정
spring.mvc.dispatch-options-request=true
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# HTTP 메소드 설정
spring.mvc.method.annotation.support=true

# CORS 설정
spring.mvc.cors.allowed-origins=https://immunology-frontend.vercel.app/
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true

# Bean Definition Overriding
spring.main.allow-bean-definition-overriding=true

# 세션 설정
server.servlet.session.timeout=3600
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# 에러 페이지 설정
server.error.whitelabel.enabled=false
server.error.path=/error

# 정적 리소스 설정
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.add-mappings=true
spring.mvc.throw-exception-if-no-handler-found=false

# 기본 경로 설정
server.forward-headers-strategy=native
spring.mvc.view.prefix=/
spring.mvc.view.suffix=.html